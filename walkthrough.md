# Walkthrough - Dev Server Log Rule

## Thay đổi thực hiện
- Đã thêm phần `6. Runtime Debugging & Diagnostics` vào `.agents/AGENTS.md`.
- Hướng dẫn Agent tự động đọc tệp tin `.md/scratch/logs/dev_server.log` khi người dùng báo cáo lỗi runtime.

## Đối soát bình luận Copilot
- Giải quyết bình luận `3540800466`: Đã sửa liên kết Markdown thành dạng inline code để tránh lỗi link hỏng (broken link) trong môi trường CI sạch (clean checkout) do thư mục `.md/scratch` bị git-ignore.
